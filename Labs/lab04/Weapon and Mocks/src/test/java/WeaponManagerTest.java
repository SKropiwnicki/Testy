import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import java.util.ArrayList;
import static org.junit.Assert.*;


public class WeaponManagerTest {
/*
    //SUT
    private IWeaponManager weaponMng;
    private IMyList mock;
    Weapon w1;
    Weapon w2;
    Weapon w3;

    @Before
    public void setUp() throws Exception {
         w1 = new Weapon("Sword", 10, 20);
         w2 = new Weapon("Axe", 11, 20);
         w3 = new Weapon("Bow", 1, 201);
        weaponMng =  new WeaponManager();
    }

    @After
    public void tearDown() throws Exception {

    }

    @Test
    public void testAdd() throws Exception {


        weaponMng.add(w1);
        ArrayList<Weapon> weapons = weaponMng.getAll();
        assertEquals(weapons.size(), 1);

        weaponMng.add(w1);
        weapons = weaponMng.getAll();
        assertEquals(weapons.size(), 2);

        weaponMng.add(w1);
        weapons = weaponMng.getAll();
        assertEquals(weapons.size(), 3);
    }

    @Test
    public void testGetAll() throws Exception {


        weaponMng.add(w1);
        ArrayList<Weapon> weapons = weaponMng.getAll();
        assertEquals(weapons.get(0).name, "Sword");
        assertEquals(weapons.get(0).damage, 10);
        assertEquals(weapons.get(0).value, 20);

        weaponMng.add(w2);
        weapons = weaponMng.getAll();
        assertEquals(weapons.get(1).name, "Axe");
        assertEquals(weapons.get(1).damage, 11);
        assertEquals(weapons.get(1).value, 20);

        weaponMng.add(w3);
        weapons = weaponMng.getAll();
        assertEquals(weapons.size(), 3);
        assertEquals(weapons.get(2).name, "Bow");
        assertEquals(weapons.get(2).damage, 1);
        assertEquals(weapons.get(2).value, 201);
    }
*/
}
